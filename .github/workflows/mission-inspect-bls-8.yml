name: mission-inspect-bls-8

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  inspect:
    runs-on: ubuntu-latest
    steps:
      - name: Inspect official BLS CPI response contract
        run: echo "Inspection retained only as a manual record; do not consume API quota automatically."
